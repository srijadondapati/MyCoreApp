@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
@page
@model MyCoreApp.Pages.IndexModel

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>MyCoreApp</title>
    <style>
        /* Example CSS with keyframes animation */
        @keyframes dropIn {
            0% { opacity: 0; transform: translateY(-30px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        .animated-element {
            animation: dropIn 1s ease-out;
        }
    </style>
</head>
<body>
    <div class="animated-element">
        <!-- Content goes here -->
    </div>
</body>
</html>
