<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sidhu - Google Style</title>
    <style>
        body {
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: white;
            font-family: 'Arial', sans-serif;
            overflow: hidden;
        }
        .name {
            display: flex;
            gap: 5px;
            font-size: 4rem;
            font-weight: bold;
            color: #4285F4; /* Google's default blue color */
        }
        .name span {
            display: inline-block;
            animation: bounce 3.5s infinite;
        }
        .name span:nth-child(1) {
            color: #4285F4; /* Blue */
            animation-delay: 0s;
        }
        .name span:nth-child(2) {
            color: #EA4335; /* Red */
            animation-delay: 0.2s;
        }
        .name span:nth-child(3) {
            color: #FBBC05; /* Yellow */
            animation-delay: 0.4s;
        }
        .name span:nth-child(4) {
            color: #34A853; /* Green */
            animation-delay: 0.6s;
        }
        .name span:nth-child(5) {
            color: #EA4335; /* Red */
            animation-delay: 0.8s;
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-50px);
            }
        }

        /* Google-styled footer */
        footer {
            position: fixed;
            bottom: 10px;
            width: 100%;
            text-align: center;
            font-size: 40px;
            color: #70757a;
        }
    </style>
</head>
<body>
    <div class="name">
        <span>S</span>
        <span>I</span>
        <span>D</span>
        <span>H</span>
        <span>U</span>
    </div>
    <footer>
        Made with <span style="color: #EA4335;">❤</span> by Donley inspired by Google
    </footer>
</body>
</html>
